{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
<div class="d-flex justify-content-center my-5">
    <div class="border border-black">
        <form method="post">
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
        
            <div class="border-bottom border-primary-subtle p-1 mx-2">
                <p>Se connecter</p>
            </div>
            <div class="p-2">
                <div class="p-2">
                    <label for="inputName">Nom utilisateur : </label>
                    <input type="name" value="{{ last_username }}" name="name" id="inputName" class="form-control" autocomplete="name" required autofocus>
                </div>
                <div class="p-2">
                    <label for="inputPassword">Password :</label>
                    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
                </div>
                <div class="p-2">
                    <input type="hidden" name="_csrf_token"
                        value="{{ csrf_token('authenticate') }}"
                    >
                    <button class="btn" type="submit">
                    Se Connecter
                    </button></br>
                </div>
                <div class="p-2 border-bottom border-primary-subtle">
                    <a href="#">Mot de passe oublié</a>
                </div>
            </div>
            <div class="py-3 px-2">
                <label for="inputBtn">Pas encore inscrit ?</label></br>
                <a class="btn" href="{{ path('app_register')}}">Créer un compte</a>
            </div>
        </form>
    </div>
</div>
{% endblock %}
